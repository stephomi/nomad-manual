# ![](/icons/scene.webp) 장면(Scene) 

이 메뉴에서는 Nomad에서 오브젝트, 라이트, 카메라, 리피터를 관리할 수 있습니다. 장면 계층 구조를 트리 뷰로 표시하여 오브젝트의 다양한 속성을 수정할 수 있습니다. 또한 새 오브젝트를 생성하고, 여러 방식으로 오브젝트를 결합하거나 분리할 수 있습니다.


![](/images/scene_menu_summary.webp)


## 단축 바
| Action                 | Icon                              | Description                                                                                                         |
| :--------------------: | :-------------------------------: | :-----------------------------------------------------------------------------------------------------------------: |
| [Add...](#add-menu)    | ![](/icons/plus.webp)            | 장면에 오브젝트를 추가하기 위해 [Add Menu](#add-menu)를 표시합니다                                                     |
| Delete                 | ![](/icons/trash.webp)           | 오브젝트를 삭제합니다                                                                                                   |
| Lock                   | ![](/icons/lock.webp)            | 뷰포트에서 오브젝트를 선택할 수 없게 만듭니다. 트리 뷰에서는 여전히 선택할 수 있습니다.                          |
| Join                   | ![](/icons/merge.webp)           | 선택된 오브젝트들을 기하 구조 변경 없이 하나의 오브젝트로 결합합니다                                             |
| Separate               | ![](/icons/diagonal.webp)        | 오브젝트가 여러 개의 독립된 폴리곤 셸로 구성된 경우, 이를 개별 오브젝트로 분리합니다. Join의 반대 작업입니다 |
| [Boolean...](#boolean) | ![](/icons/subtract_circle.webp) | [Boolean](#boolean) 메뉴를 표시합니다                                                                                |
| Clone                  | ![](/icons/clone.webp)           | 오브젝트를 복제하여 새 오브젝트를 만듭니다                                                                              |
| Instance               | ![](/icons/link.webp)            | 오브젝트를 인스턴스로 복제하여, 하나를 수정하면 모든 인스턴스에 모델링 변경이 반영되도록 합니다.                         |
| Un-instance            | ![](/icons/unlink.webp)          | 인스턴스를 고유한 형태로 변환하여, 이후 모델링 변경이 다른 인스턴스에 더 이상 복사되지 않도록 합니다                 |
| Sync                   | ![](/icons/link.webp)            | 인스턴스에 자식이 있는 경우, 모든 인스턴스가 동일한 자식 계층 구조를 공유하도록 동기화합니다                                     |


## 트리 뷰
![](/images/scene_treeview.webp) 

| Action       | Icon                       | Description              |
| :----------: | :------------------------: | :----------------------: |
| Select       | ![](/icons/checked.webp)  | 선택/선택 해제 토글 |
| Visible      | ![](/icons/eye_open.webp) | 표시/숨김 토글        |
| Menu         | ![](/icons/more.webp)     | 오브젝트 메뉴 표시      |

::: tip TIP: 여러 오브젝트를 빠르게 선택하거나 숨기기

선택 아이콘을 탭하면 단일 오브젝트를 토글하고, 선택 열에서 세로로 드래그하면 여러 오브젝트를 한 번에 선택할 수 있습니다. 표시 열에서도 동일하게 사용할 수 있습니다.

:::

### 트리 뷰 조작

트리 뷰의 항목을 길게 눌러 노란색으로 변할 때까지 유지합니다. 그러면 트리 뷰에서 위아래로 이동할 수 있고, 다른 항목 위로 드래그하여 그 항목의 자식으로 만들 수도 있습니다.

여러 항목이 선택된 경우 대부분은 어두운 노란색, 하나는 더 밝은 노란색으로 표시됩니다. 밝은 항목을 길게 눌러 드래그하면 모든 오브젝트를 한 번에 이동할 수 있습니다.

부모 항목을 선택하면 기본적으로 모든 자식 항목도 함께 선택됩니다. 부모 아이콘을 탭하면 부모만 선택할지, 부모와 자식을 함께 선택할지 토글됩니다.

### 오브젝트 메뉴

트리 뷰에서 오브젝트의 줄임표 버튼(...)을 클릭하면 오브젝트 메뉴가 표시됩니다.  
이 옵션들 중 상당수는 상단 단축 바와 유사하며, 편의를 위해 반복 제공됩니다.

|       Action        |                        Icon                        | Description                                                                                                                                                             |
| :-----------------: | :------------------------------------------------: | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|      Instance       |               ![](/icons/link.webp)            | 오브젝트를 인스턴스로 복제하여, 하나를 수정하면 모든 인스턴스에 모델링 변경이 반영되도록 합니다.                                                                             |
|        Clone        |              ![](/icons/clone.webp)            | 오브젝트를 복제하여 새 오브젝트를 만듭니다                                                                                                                                  |
|        Name         |              ![](/icons/pencil.webp)           | 오브젝트의 이름을 변경합니다                                                                                                                                           |
|       Delete        |              ![](/icons/trash.webp)            | 오브젝트를 삭제합니다                                                                                                                                                       |
|       Delete+       |            ![](/icons/removeNode.webp)         | 오브젝트와 그 자식을 모두 삭제합니다                                                                                                                                      |
|     Un-instance     |              ![](/icons/unlink.webp)           | 인스턴스를 고유한 형태로 변환하여, 이후 모델링 변경이 다른 인스턴스에 더 이상 복사되지 않도록 합니다.                                                                    |
|  Separate Topology  |             ![](/icons/separate.webp)          | 오브젝트가 여러 개의 독립된 폴리곤 셸로 구성된 경우, 이를 개별 오브젝트로 분리합니다. Join 작업의 반대입니다.                                                    |
| Separate Face Group |            ![](/icons/faceGroup.webp)          | 오브젝트에 여러 페이스 그룹이 있는 경우, 메시를 개별 오브젝트로 분리합니다.                                                                                            |
|   Separate Layers   |              ![](/icons/layer.webp)            | 오브젝트에 레이어가 있는 경우, 각 레이어를 개별 오브젝트로 분리합니다. 블렌드셰이프를 다른 애플리케이션으로 보낼 때 유용합니다.                                                 |
|   Join -> Layers    | ![](/icons/merge.webp) -> ![](/icons/layer.webp) <span style="visibility:hidden">_________</span> | 여러 오브젝트가 선택되어 있고 토폴로지가 일치하는 경우, 이 오브젝트들을 기본 오브젝트의 레이어로 병합합니다(다른 오브젝트는 삭제됨). 마찬가지로, 다른 애플리케이션에서 넘어온 블렌드셰이프에 유용합니다.<br><br> 레이어는 기본적으로 비활성화되어 있습니다. 슬라이더를 조정하려면 활성화해야 합니다. |




### 다중 선택(Multiselection)
여러 오브젝트를 선택하면 다음 두 가지를 할 수 있습니다:
- 기즈모 도구를 사용해 여러 오브젝트를 한 번에 이동
- Join 및 Boolean 연산으로 오브젝트 병합

이를 위해 `Multiselection` 체크박스를 사용한 뒤, 목록에서 오브젝트를 클릭하면 됩니다.

::: tip 빠른 다중 선택
뷰포트에서 `Smooth` 단축키를 누른 상태로 다른 메시를 탭하면 다중 선택할 수 있습니다.

선택된 오브젝트가 두 개 이상일 때, 다시 탭하면 해당 오브젝트를 선택 해제할 수 있습니다.
:::

::: warning 제한된 기즈모 기능
다중 선택을 사용할 때 기즈모 도구는 항상 마스킹을 무시합니다.  
또한 X/Y/Z 스케일링 기능이 제거됩니다.

이는 다중 선택이 버텍스 단위 변형이 아닌, 전체 메시 단위 변형만 허용하기 때문입니다.  
향후 개선될 수 있습니다.
:::


## Join
이 옵션은 단순히 여러 개의 선택된 오브젝트를 하나의 오브젝트 항목으로 만듭니다.

예시는 [Separate](#separate) 섹션의 영상에서 확인할 수 있습니다.

## Boolean
![](/images/scene_boolean_menu.webp)  
여러 오브젝트를 하나의 표면으로 결합합니다.

`Voxel merge`는 오브젝트의 부피를 유지하면서, 표면에 균일하게 분포된 새로운 폴리곤을 계산합니다. 계산 단계가 있기 때문에 복잡한 기하 구조도 처리할 수 있지만, 목표 해상도가 충분히 높지 않으면 세밀한 디테일이 손실될 수 있습니다.

`Boolean`은 가능한 한 기존 폴리곤 배치를 유지하면서, 오브젝트가 겹치는 부분의 폴리곤을 봉합하려고 시도합니다. 이는 보통 보xel 머지보다 훨씬 더 깔끔하고 날카로운 결과를 만들지만, ‘watertight’ 메시가 필요합니다. 즉, 오브젝트에 구멍이나 잘못된 형태가 있으면 안 됩니다. 이 작업이 실패할 경우, 보통 Voxel merge는 동작합니다.

### Boolean 연산
Voxel Merge와 Boolean 모두 오브젝트의 가시성을 사용해 연산 방식을 제어합니다:

#### Union
두 오브젝트가 모두 보이는 상태이면 Boolean **Union**이 수행되며, 오브젝트의 외피가 결합되고 내부 표면은 없습니다. ![](/images/boolean_union.webp)

#### Subtract
한 오브젝트가 보이지 않는 상태이면 Boolean **Subtract**가 수행되며, 보이지 않는 오브젝트가 보이는 오브젝트에서 빼기 연산됩니다. ![](/images/boolean_subtract.webp)

#### Intersect
두 오브젝트가 모두 보이지 않는 상태이면 Boolean **Intersection**이 수행되며, 두 오브젝트가 겹치는 부분만 새로운 형태로 생성됩니다. ![](/images/boolean_intersect.webp)


### Voxel Merge 버튼
이 버튼을 누르면 선택된 오브젝트에 대해 Voxel merge 연산을 수행합니다. 단일 오브젝트에 사용하면, 늘어난 폴리곤을 균일한 폴리곤으로 리토폴로지하는 데 유용합니다.

### Resolution
계산에 사용되는 보xel 3D 그리드의 해상도입니다. 이 값을 변경하면, 폴리곤 크기를 미리 보기 위해 오브젝트 위에 체커보드 패턴이 오버레이됩니다.

### Build multiresolution
목표 해상도 아래에 멀티 해상도 레벨을 생성합니다. 예를 들어 해상도가 400이고 build multiresolution 값이 3이면, 약 296,000 폴리곤의 새 메시가 생성되며, 그 아래에 74,000, 18,000, 4,000 폴리곤 정도의 3개 서브디브 레벨이 생깁니다.

### Keep sharp edges
보xel 메시를 엣지에 스냅하도록 활성화합니다. 단순한 형태에서 가장 잘 동작합니다.

### Boolean 버튼
이 버튼을 누르면 Emmett Lalish의 Manifold 라이브러리를 사용해 폴리곤 Boolean 연산을 수행합니다. 


## Separate
여러 개의 분리된 파트로 이루어진 단일 오브젝트가 있는 경우, 이를 여러 오브젝트로 분리할 수 있습니다.  
이는 [Simple Merging](#simple-merge)의 반대 작업으로 볼 수 있습니다.

![](/videos/merge_separate.mp4)


## Add 메뉴

![](/images/scene_addmenu_overview.webp)

이 메뉴에서는 프리미티브, 그룹, 카메라, 리피터, 라이트를 생성합니다.

프리미티브는 파라미터로 조정 가능한 기본 형태입니다. 프리미티브를 원하는 형태로 조정한 뒤 ‘Validate’를 하면, 이 파라미터들이 조각 및 페인팅이 가능한 폴리곤 메시로 굳어집니다. Validate 이후에는 프리미티브의 파라미터를 더 이상 조정할 수 없습니다.


![](/images/scene_addmenu_top.webp)

### On gizmo
새 프리미티브를 현재 선택된 형태 또는 기즈모 위치에 배치하도록 활성화합니다. 비활성화하면 프리미티브는 장면의 중심에 배치됩니다.

### Select gizmo
새 프리미티브가 생성될 때 자동으로 기즈모 도구로 전환되도록 활성화합니다. 

### Advanced

이 메뉴에서는 새 프리미티브, 그룹, 리피터를 어디에 생성할지에 대한 기본 설정을 지정할 수 있습니다. 선택된 오브젝트, 월드 원점, 또는 기즈모 위치 중에서 선택할 수 있습니다.


### UV's
프리미티브에 UV를 활성화합니다. UV(텍스처 좌표라고도 함)는 표면에 텍스처를 적용하기 위해 3D에서 사용하는 추가 데이터입니다. 메모리를 더 사용하지만, 대부분의 기기에서는 매우 높은 폴리곤 수(예: 1,000만 폴리곤 이상)가 아니라면 큰 문제가 되지 않습니다. 

### 프리미티브

| Primitive      | Icon                                      | Description                                                                                                     |
| :------------: | :---------------------------------------: | :-------------------------------------------------------------------------------------------------------------: |
| Box            | ![](/icons/cube.webp)                    | 단순한 큐브이며, X, Y, Z 방향의 분할 수를 제어할 수 있습니다                                                  |
| Sphere         | ![](/icons/circle.webp)                  | 편의를 위해 Sphere라고 부르지만 실제로는 `Project on sphere`가 강제된 서브디바이드 박스입니다 |
| Cylinder       | ![](/icons/cylinder.webp)                | 실린더 프리미티브에 중심 구멍을 추가할 수 있어, 예를 들어 속이 빈 파이프를 만들 수 있습니다                        |
| Torus          | ![](/icons/torus.webp)                   | 토러스는 반지를 만들 때 좋은 시작점이 됩니다                                                                |
| Cone           | ![](/icons/cone.webp)                    | -                                                                                                               |
| Icosahedron    | ![](/icons/icosahedron.webp)             | -                                                                                                               |
| UV-sphere      | ![](/icons/circle.webp)                  | 불균일한 폴리곤 배치를 가진 구체입니다. [아래 경고](#uv-sphere) 참고                                               |
| Plane          | ![](/icons/rectangle.webp)               | 단순한 평면이며, 유일하게 닫혀 있지 않은 프리미티브입니다                                    |
| Tube           | ![](/icons/tool_tube.webp)               | [Tube](tools#tube) 참고                                                                                          |
| Lathe          | ![](/icons/tool_lathe.webp)              | [Lathe](tools#lathe) 참고                                                                                        |
| Triplanar      | ![](/icons/triplanar.webp)               | [Triplanar](#triplanar) 참고                                                                                     |
| Shadow Catcher | ![](/icons/material_shadow_catcher.webp) | [Shadow Catcher](#shadow-catcher) 참고                                                                           |
| Head           | ![](/icons/face.webp)                    | 남/여를 블렌딩할 수 있는 레이어가 포함된 단순한 머리 형태                                                         |

::: tip
Nomad를 실행했을 때 기본으로 나오는 베이스 메시가 무엇인지 궁금하다면: 이것 역시 서브디바이드 박스입니다.  
다만 Nomad의 베이스 메시는 `Project on sphere`를 사용하지 않기 때문에 완전히 둥글지는 않습니다.
:::

### 프리미티브 툴바

![](/images/scene_primitive_toolbar.gif)

프리미티브를 생성하면, 해당 파라미터를 제어하는 툴바가 나타납니다.

* `Validate` 프리미티브를 일반 오브젝트로 굳혀 조각 및 페인팅이 가능하게 합니다.
* `Edit` 프리미티브 기즈모 표시를 토글합니다. 이는 큐브의 폭, 실린더의 구멍 반지름 등 파라미터를 제어하기 위해 프리미티브 위에 직접 표시됩니다.
* `Mirror` 프리미티브 위에 미러 리피터를 배치할지 토글합니다.
* `...` 프리미티브 메뉴를 표시합니다.

프리미티브 종류에 따라 툴바에 추가 옵션이 있을 수 있습니다:

* `Project` Sphere는 조각에 더 적합한 서브디바이드 큐브 기반으로 구성되지만, 이 때문에 완전한 구형은 아닙니다. 이 옵션은 형태를 더 완벽한 구에 가깝게 강제합니다. Icosahedron도 이 옵션을 공유합니다.
* `Cap` 형태의 끝면 캡을 토글합니다. 예를 들어 실린더의 위/아래/양쪽/없음 등을 설정할 수 있습니다.
* `Hole` 중심을 관통하는 구멍을 생성할지 토글합니다. 구멍 없음, 단일 반지름 구멍, 위/아래 반지름이 다른 구멍을 순환합니다.
* `Radius` 실린더가 단일 반지름을 가질지, 위/아래에 서로 다른 반지름을 가질지 토글합니다.
* `Disk` Plane을 4각형으로 할지, 원형으로 할지 토글합니다.

메인 툴바 아래의 작은 툴바에서는 프리미티브 기즈모와 트랜스폼 기즈모 사이를 전환할 수 있습니다.

::: tip

툴바의 제목을 클릭하면 화면 상단/하단으로 위치를 토글합니다. 모서리의 화살표를 클릭하면 툴바를 접을 수 있습니다.

:::


### 프리미티브 메뉴

![](/images/scene_primitive_menu.webp)

선택된 프리미티브의 모든 파라미터가 포함되어 있습니다. 파라미터는 형태를 기술하는 기본 값들입니다. 예를 들어 반지를 기술하려면, 바깥 반지름, 안쪽 반지름, 폴리곤 수 등을 설명하게 됩니다.

대부분의 프리미티브 파라미터는 직관적이며, 모든 프리미티브에 공통으로 존재하는 파라미터도 있습니다:

* `UVs` 메뉴 상단의 작은 UVs 버튼은 UV 좌표 생성 여부를 토글합니다.
* `Validate` 작은 Validate 버튼은 프리미티브를 일반 오브젝트로 굳혀 조각 및 페인팅이 가능하게 합니다.
* `Max faces` 기기가 다운되지 않도록 오브젝트의 최대 폴리곤 수 상한을 설정합니다.
* `Post subdivision` 토폴로지 메뉴의 멀티 해상도 섹션에서 선택한 서브디브 레벨 수를 적용합니다. 낮은 토폴로지 분할과 조합해 모서리가 부드러운 프리미티브를 만드는 데 사용할 수 있습니다. 예를 들어 Box의 토폴로지 분할을 2로, Post subdivision을 4로 설정하면 모서리가 부드러운 박스가 됩니다.
* `Linear subdivision` 일반 스무스 서브디브 전에 사용할 선형 서브디브 레벨 수를 설정합니다. 이는 서브디바이드된 표면의 모서리가 얼마나 날카롭거나 부드러운지 제어하는 데 사용됩니다. 예를 들어 Box의 토폴로지 분할을 2, Post subdivision을 4로 설정한 뒤, Linear subdivision을 0에서 4까지 변경해 보면 박스 모서리가 부드러운 상태에서 날카로운 상태로 변하는 것을 볼 수 있습니다.

### Topology

프리미티브의 폴리곤 수를 제어합니다. 보통 컨트롤은 연결되어 있어, 활성 슬라이더 하나를 변경하면 전체 폴리곤 수가 균일하게 조정됩니다. 링크 해제 버튼을 탭하면 X/Y/Z 분할을 개별적으로 제어할 수 있습니다.

### Geometry

프리미티브의 전체 크기를 제어합니다. 사각형 형태는 X/Y/Z 단위, 원형 형태는 반지름으로 제어합니다.


### UV Sphere
::: warning
UV Sphere는 특히 극(폴) 부분에서 조각용으로 적합하지 않습니다.

`Project on sphere` 옵션과 함께 [Sphere](#sphere), [Box](#box), [Icosahedron](#icosahedron) 프리미티브를 사용하는 것을 권장합니다.

`Division` 슬라이더 값을 매우 낮게 설정하면 조각에 적절한 토폴로지를 얻을 수 있습니다.  
그 후 `Overall Subdivision` 슬라이더를 사용해 폴리곤 수를 늘릴 수 있습니다.

일반적인 조각에는 적합하지 않지만, 눈을 만들 때는 유용합니다. 구체를 회전시켜 극이 동공 위치에 오도록 하면, 폴리곤 배치가 자연스럽게 홍채와 동공을 페인팅/조각하기에 알맞게 정렬됩니다.
:::


### Triplanar
이 프리미티브는 [Masking 도구](tools.md#mask)를 사용해 기하 구조를 형성해야 한다는 점에서 특별합니다.

![](/videos/triplanar.mp4)


::: tip
평면을 더블 탭하면 카메라가 해당 평면에 포커스를 맞춥니다.  
단, 기즈모로 프리미티브를 회전한 경우에는 동작하지 않습니다.
:::

Triplanar는 3개의 평면에서 마스크 정보를 사용해 보xel 그리드를 채우고, 이를 [Voxel Remesher](topology.md#voxel-remeshere)를 통해 폴리곤화합니다.

각 평면은 고유한 대칭 평면을 가집니다.

::: warning
Triplanar 프리미티브의 크기를 업데이트할 때마다 마스크 페인팅 품질이 저하됩니다.

현재는 단일 평면의 페인팅을 ‘고정’하는 옵션이 없지만, 향후 추가될 수 있습니다.  
커서를 정확히 한 평면 위에 둘 경우 다른 평면에 영향을 주지 않도록 돕는 [Connected Topology](stroke.md#connected-topology)를 어느 정도 활용할 수 있습니다.
:::

### Shadow Catcher
Shadow catcher 머티리얼이 적용된 평면을 추가합니다. 자세한 내용은 [Shadow Catcher material](material.md#shadow-catcher)을 참고하세요. 


## Group/Camera
### Group
다른 오브젝트를 자식으로 둘 수 있는 ‘빈’ 오브젝트를 생성합니다. 여러 오브젝트를 그룹 아래에 두고 접어서 계층 구조를 정리하는 데 사용할 수 있습니다. 또한 그룹을 기즈모 도구로 이동/회전/스케일링하여, 그 아래의 여러 오브젝트를 한 번에 조작하는 헬퍼로 사용할 수도 있습니다.

### Add view
카메라를 생성합니다.

## Repeaters
![](/images/scene_primitive_repeaters.webp)

리피터는 그 아래에 있는 오브젝트의 인스턴스를 생성하는 노드입니다. 

### Array
![](/images/scene_primitive_array.webp)

이 노드의 자식이 된 오브젝트는 그리드 형태로 인스턴싱할 수 있습니다. 선택 시 다음과 같은 컨트롤이 있습니다:
* Fit inside - 배열 인스턴스 간의 간격을 제어할지, 배열 전체의 그리드/박스 크기를 제어할지 토글
* CountX/Y/Z - 각 축 방향의 인스턴스 개수
* OffsetX/Y/Z - Fit inside가 꺼져 있을 때 인스턴스 간 거리
* SizeX/Y/Z - Fit inside가 켜져 있을 때 배열 그리드 전체의 너비/높이/깊이

### Curve
![](/images/scene_primitive_curve.webp)  
곡선을 생성하며, 이 노드의 자식이 된 오브젝트는 곡선을 따라 반복됩니다. 선택 시 다음과 같은 컨트롤이 있습니다:
* Edit - 곡선에 포인트를 추가하고, 곡선 위의 포인트를 이동할 수 있게 합니다.
* Snap - 곡선 포인트를 다른 기하 구조에 스냅합니다.
* Align - 자식 형태를 곡선 방향에 맞춰 회전시킵니다.
* Count - 인스턴스 개수
* Closed - 곡선의 시작과 끝을 연결할지, 열린 곡선으로 둘지 토글
* Radius - 각 곡선 포인트에 인스턴스의 스케일을 제어하는 컨트롤을 토글
* Twist - 각 곡선 포인트에 인스턴스의 비틀림 회전을 제어하는 컨트롤을 토글
* B-spline - 인스턴스를 곡선을 정확히 따르게 할지, 더 부드러운 결과를 내는 B-spline 보간을 사용할지 토글 

### Radial
![](/images/scene_primitive_radial.webp)

이 노드의 자식이 된 오브젝트는 원형으로 인스턴싱됩니다. 자식 오브젝트를 이동하면 리피터의 반지름이 변경됩니다. 선택 시 다음과 같은 컨트롤이 있습니다:
* RadialX/Y/Z - 이 버튼들로 회전 축을 설정하고, 인스턴스 개수를 지정합니다.



### Mirror
![](/images/scene_primitive_mirror.webp)

이 노드의 자식이 된 오브젝트는 축을 기준으로 미러링됩니다. 선택 시 다음과 같은 컨트롤이 있습니다:
* Gizmo - 트랜스폼 기즈모를 활성화하여 미러 중심을 설정합니다. 회전 및 스케일도 가능합니다. 완료 후 다시 Gizmo를 탭하면 기본 컨트롤이 표시됩니다.
* X/Y/Z - 미러 평면을 설정합니다.

모든 리피터에는 `Validate` 컨트롤이 있으며, 리피터 결과를 굳힐 때 어떻게 처리할지 묻습니다:
* Join children - 인스턴스를 하나의 오브젝트로 결합
* Keep instances - 인스턴스를 유지하되, 더 이상 리피터의 ‘부모’ 아래에 두지 않음
* Un-instances - 인스턴스를 고유한 오브젝트로 변환

::: tip Tip: 리피터 결합
리피터는 서로 부모-자식 관계를 맺을 수 있고, 여러 오브젝트를 리피터의 자식으로 둘 수 있어 복잡한 효과를 만들 수 있습니다.

![](/images/scene_repeater_combine.webp)
:::

::: tip Tip: 리피터 피벗

일부 리피터는 자식 오브젝트의 피벗을 자동으로 조정하려고 하므로, 기즈모 도구로 자식을 이동/회전해도 움직이지 않는 것처럼 보일 수 있습니다. 이 동작을 무시하려면 리피터와 자식 사이에 그룹을 하나 끼워 넣으세요. 그러면 리피터와 독립적으로 자식 형태를 이동할 수 있습니다.
:::

## Light

![](/images/scene_primitive_light.webp)

### Directional
태양처럼 무한히 멀리 있는 광원인 방향성 라이트를 생성합니다.

### Spot
콘 너비와 부드러움을 제어할 수 있는 스포트 라이트를 생성합니다.

### Point
포인트 라이트를 생성합니다.

## Advanced
### Focus on item
Scene 목록에서 항목을 더블 클릭하면 3D 뷰에서 해당 항목을 중심으로 카메라가 이동합니다.

### Sync visibility
눈 아이콘을 사용하면 선택된 모든 항목에 표시/숨김이 적용됩니다. 

### Instance: Show
Scene 목록 왼쪽에 색상 캡슐을 표시하여 인스턴스를 구분합니다.


### Icons
뷰포트에서 그룹, 라이트, 카메라, 미러 아이콘의 크기와 불투명도를 설정합니다.

### Hierarchy lines
뷰포트에서 부모와 자식 사이에 선을 표시합니다.

## 하단 툴바
이 아이콘들은 뷰포트에서 Group, Light, Camera, Repeater, Hierarchy line의 표시 여부를 토글합니다.
